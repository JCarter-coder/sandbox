var maximumGain = function(s, x, y) {
    let score = 0;
    if (x > y) {
        // remove "ab"
    } else {
        // remove "ba"
    }
};

maximumGain("cdbcbbaaabab",4,5);
maximumGain("aabbaaxybbaabb",5,4);
